# Non-secret PostgreSQL runtime configuration.
# The password itself is supplied through the Podman secret
# `akamai-mfa-pg-v2-password` mounted at /run/secrets/pg_password.

POSTGRES_USER=CHANGE_ME
POSTGRES_PASSWORD_FILE=/run/secrets/pg_password
PGDATA=CHANGE_ME
POSTGRES_DB=CHANGE_ME
